# 开发规范（require.md）

> 修改项目时必须遵守的规则。改完代码后对照本清单自查，再提交。

## 一、结构与维护

1. 修改项目时要考虑项目的可扩展性和维护性，确保在未来可以方便地添加新功能和修复 bug。
2. 修改项目时要考虑需不需要新建文件夹或文件来组织项目的代码和文件，不要所有的代码都放在一个文件里。
3. 修改项目时要考虑项目的结构和组织方式，确保代码易读、易维护：命名清晰、关键逻辑加注释。
4. 项目采用三层分层架构：`blueprints/`（路由/HTTP 层）、`services/`（纯业务逻辑层）、`static/`（前端资源）。新增功能时遵循此结构，不跨层耦合。

## 二、前端与移动端

1. 这个项目是主打手机端的，要做好手机端和平板电脑端的适配。
2. 所有页面均通过 `nav.html` 共享侧边栏导航；新增页面时要在 `nav.html` 中添加对应入口。
3. 弹窗统一使用自定义实现（`theme.js` 中的 `toast()` / `confirmBox()` / `promptBox()`），不使用原生 `alert()` / `confirm()` / `prompt()`。

## 三、代码与安全

1. 所有文件系统操作必须经过 `services/path_utils.safe_path()` 做路径安全校验（防目录穿越），不直接拼接路径字符串。
2. 新增存储模块（如日志/待办/备忘录）使用 JSON 文件 + 线程锁的原子写入模式，参考现有 `*_store.py` 实现。
3. 用户输入在渲染前需转义（防 XSS），图片上传需校验扩展名白名单和文件大小。

## 四、删除测试文件

1. 项目开发完成后，要删除所有的测试文件，包括测试用的代码、测试数据、测试配置文件等。
2. 要删除所有的测试用的文件夹，包括测试用的目录、测试用的文件等。


