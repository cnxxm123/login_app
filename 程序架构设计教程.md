# 程序架构设计教程 —— 以本项目（云书库）为例

> 面向对象：没做过实际项目、想学会"程序怎么组织起来"的初学者。
> 方法：不看抽象理论，直接解剖本项目的真实代码，看懂"每一层是干什么的、为什么这么分"。
> 建议：边读边打开项目文件对照，效果最好。

---

## 目录

1. [先回答一个问题：程序架构到底是什么？](#1-先回答一个问题程序架构到底是什么)
2. [项目全景图：三层分层](#2-项目全景图三层分层)
3. [第一层：入口与配置（app.py / config.py / db.py）](#3-第一层入口与配置apppy--configpy--dbpy)
4. [第二层：路由处理层（blueprints 包）](#4-第二层路由处理层blueprints-包)
5. [第三层：纯业务逻辑层（services 包）](#5-第三层纯业务逻辑层services-包)
6. [前端层：templates + static](#6-前端层templates--static)
7. [一次请求的完整旅程](#7-一次请求的完整旅程)
8. [贯穿全项目的四大设计原则](#8-贯穿全项目的四大设计原则)
9. [实战演练：给项目加一个新功能](#9-实战演练给项目加一个新功能)
10. [从本项目学到的通用方法论](#10-从本项目学到的通用方法论)
11. [官方资料推荐](#11-官方资料推荐)

---

## 1. 先回答一个问题：程序架构到底是什么？

一句话：**架构 = 把代码按职责分到不同"房间"，并规定"房间之间怎么串门"。**

想象你写了一个 5000 行的程序，全塞在一个文件里。它能跑，但：
- 想改"搜索功能"，要在一个 5000 行的文件里翻找，还可能改错别的地方；
- 想让"搜索功能"测试一下，发现它和"上传功能"纠缠在一起，没法单独测；
- 来一个新同事，看半天不知道从哪看起。

**架构就是提前解决这些问题的**：把代码按"各自负责什么"拆开，并规定依赖方向。本项目就是一个活例子，它的架构只有三层：

```
┌─────────────────────────────────────────────┐
│  浏览器（用户看到的东西）                       │
└─────────────────────────────────────────────┘
                     │ HTTP 请求
                     ▼
┌─────────────────────────────────────────────┐
│  blueprints/  路由处理层：收请求、调服务、回页面 │
│  （browser / view / media / download / ...）  │
└─────────────────────────────────────────────┘
                     │ 调用函数
                     ▼
┌─────────────────────────────────────────────┐
│  services/    纯业务逻辑层：不碰 HTTP          │
│  （列目录 / 读文件 / 抽帧 / 路径校验 / ...）    │
└─────────────────────────────────────────────┘
```

**关键规律（记住这条，就学会了一半）：依赖是单向的 —— 上层调用下层，下层绝不知道上层的存在。**

- `blueprints` 可以 `import services`（路由层调用逻辑层）
- `services` 绝对不能 `import blueprints`（逻辑层不知道路由层的存在）
- 这样每一层都能单独替换、单独测试，改 A 不炸 B。

下面逐层解剖。

---

## 2. 项目全景图：三层分层

先看项目根目录长什么样（对照你的 [README.md](README.md)）：

```
login_app/
├── app.py                 # 第 1 层-入口：组装并启动
├── config.py              # 第 1 层-配置：所有"可变的信息"集中放这里
├── db.py                  # 第 1 层-数据库初始化
├── blueprints/            # 第 2 层-路由处理层
│   ├── browser.py         #   浏览：主页 / 目录 / 搜索
│   ├── view.py            #   查看：文档 / 图片 / 视频查看页
│   ├── media.py           #   媒体：二进制流返回 + 缩略图
│   ├── download.py        #   下载：单文件 / 目录打包 zip
│   ├── manage.py          #   管理：上传 / 新建文件夹 / 在线编辑
│   ├── logs.py            #   日志：工作日志增删改
│   └── todos.py           #   待办：待办事项增删改
├── services/              # 第 3 层-纯业务逻辑层
│   ├── path_utils.py      #   路径安全校验（防目录穿越）
│   ├── dir_utils.py       #   列目录 / 排序 / 搜索 / 图片列表
│   ├── text_utils.py      #   读文本 / Markdown 渲染
│   ├── media_utils.py     #   视频抽帧 / 转码 / 图片缩略图
│   ├── office_utils.py    #   Office 文档解析
│   ├── log_store.py       #   工作日志读写（JSON）
│   └── todo_store.py      #   待办事项读写（JSON）
├── templates/             # 前端：Jinja2 模板（HTML）
├── static/                # 前端：CSS / JS
└── text/                  # 业务数据：被浏览的内容目录
```

**只看这张图你能获得什么信息？**
1. 每个文件名字都带"职责"（browser=浏览、download=下载）—— 一眼知道去哪找代码；
2. 两个包 `blueprints` / `services` 各自装同一类职责 —— 知道代码在哪一层；
3. 依赖方向：模板 → 蓝图 → 服务，箭头永远向下。

> 这就是"约定大于配置"：架构设计好之后，**放新代码的位置是固定的**，不用纠结。

---

## 3. 第一层：入口与配置（app.py / config.py / db.py）

### 3.1 app.py —— 项目的大门

打开 [app.py](app.py)，它做的事情只有三件：

1. **创建应用**：`app = Flask(__name__)`
2. **注册蓝图**：把每个功能的"路由器"装到应用上（`app.register_blueprint(...)`）
3. **启动**：`app.run(host="0.0.0.0", port=5000)`

重点看这个设计：

```python
def create_app() -> Flask:
    """应用工厂：创建应用并注册蓝图。"""
    app = Flask(__name__)
    ...
    app.register_blueprint(browser_bp)   # 注册浏览蓝图
    app.register_blueprint(view_bp)      # 注册查看蓝图
    ...
    return app

app = create_app()   # 模块级直接创建实例
```

**它用了"应用工厂模式"（Flask 官方推荐）**。为什么要包一层函数而不是直接写？
- 测试时可以 `create_app()` 造一个新的干净应用，测完扔掉，互不污染；
- 以后想跑两个配置不同的实例，直接调用两次即可；
- 这是 Flask 官方文档的"Application Factories"章节讲的标准做法，你以后写 Flask 项目可以照抄这个骨架。

### 3.2 config.py —— 所有"会变的信息"集中管理

打开 [config.py](config.py)。这里放着：
- 浏览根目录路径 `TEXT_DIR`
- 各种扩展名表 `TEXT_EXTENSIONS` / `IMAGE_EXTENSIONS` / `VIDEO_EXTENSIONS`...
- 上传上限、缓存目录、MIME 映射

**设计思想：把"可变信息"和"程序逻辑"分离。** 比如以后你想换一个浏览目录，只改一行 `CONTENT_PATH = r"F:\sex"`，重启即可，**不用碰任何逻辑代码**。

```python
TEXT_DIR = (
    (CONTENT_PATH if os.path.isdir(CONTENT_PATH) else "")
    or os.environ.get("TEXT_DIR")
    or os.path.join(BASE_DIR, "text")
)
```

这行还展示了一个"优雅降级"技巧：配置路径不存在 → 读环境变量 → 都没有就用项目内 `text/`。程序永远有一个兜底值，不会崩。

> 你可能注意到扩展名表用的是 `set` 而不是 `list`。注释里写了原因：判断"是否包含"时 set 是 O(1)，list 是 O(n)。**代码里的每个小选择都有理由，这就是"有意识编程"。**

### 3.3 db.py —— 数据库初始化

本项目目前数据库只用来"建库"（[db.py](db.py)）。注意它的"幂等"设计：

```python
cur.execute(f"CREATE DATABASE IF NOT EXISTS `{DB_NAME}` ...")
```

`IF NOT EXISTS` 让建库操作可以重复执行而不出错 —— 所以每次启动都能安全调用，这叫**幂等**（idempotent）。真实项目里建表、跑迁移脚本都讲究幂等。

---

## 4. 第二层：路由处理层（blueprints 包）

### 4.1 什么是蓝图？为什么每个功能一个蓝图？

蓝图（Blueprint）是 Flask 提供的"路由分组"机制。**一个蓝图 = 一组相关路由 + 一个名字。**

本项目把功能切成 7 个蓝图：

| 蓝图 | 名字（url_for 用） | 负责的路由 | 职责 |
| --- | --- | --- | --- |
| browser | `browser` | `/` `/browse/<path>` `/search` | 浏览页面 |
| view | `view` | `/view/<path>` | 查看页面 |
| media | `media` | `/media/<path>` `/thumb/<path>` | 返回文件流 |
| download | `download` | `/download/<path>` | 下载 |
| manage | `manage` | `/upload` `/mkdir` `/edit` `/save` | 上传/新建/编辑 |
| logs | `logs` | `/logs` `/log/add`... | 工作日志 |
| todos | `todos` | `/todos` `/todo/add`... | 待办事项 |

**好处（对比所有路由堆在一个文件）：**
- 找代码快：看到 `/download/...` 相关 bug，直接进 `download.py`；
- 不打架：两个人同时改浏览和下载，不会改到同一个文件冲突；
- 可插拔：哪天不要"待办"功能了，删掉蓝图和注册一行就行（本项目删除"游戏""回收站"功能就是这么干的）。

### 4.2 一个蓝图的解剖：看 browser.py 怎么干活

打开 [browser.py](blueprints/browser.py)，它内部是标准的"三层动作"：

```python
@browser_bp.route("/browse/<path:subpath>")     # ① 收请求（带参数）
def browse(subpath: str):
    """浏览某个文件夹。"""
    return render_browse(subpath)               # ③ 返回响应（页面）
```

而真正干活的是 `render_browse` → `browse_context`：

```python
def browse_context(subpath: str) -> dict | None:
    entries = list_entries(subpath)             # ② 调 services 层干活
    if entries is None:
        return None
    ...
    return {
        "path": subpath,
        "parent": parent,
        "breadcrumbs": crumbs,
        "dirs_cover": dirs_cover,
        "dirs_plain": dirs_plain,
        "files": file_entries,
    }
```

**注意看它怎么处理"边界情况"**：`list_entries` 返回 `None`（路径非法）时，`browse_context` 返回 `None`，上层 `abort(404)`。这个"返回值表示失败"的约定，让错误处理层层传递、不抛异常。

再看 `render_browse` 里的一行：

```python
return render_template("main.html", max_form_parts=..., **ctx)
```

`**ctx` 把字典展开成模板变量 —— 模板层和逻辑层通过一个"上下文 dict"解耦：**逻辑层只负责"准备数据"，不关心数据怎么显示。**

### 4.3 路由层的"通用保护"：路径安全校验

浏览、查看、下载、媒体四个蓝图，几乎每个路由第一行都是：

```python
target = safe_path(subpath)   # 防目录穿越
if target is None:
    abort(404)
```

这就是 [path_utils.py](services/path_utils.py) 的 `safe_path`。**同一个安全函数被所有入口复用** —— 这就是"把安全逻辑收敛到一个地方"的价值：只要这一处是对的，所有入口都安全。

---

## 5. 第三层：纯业务逻辑层（services 包）

### 5.1 这一层的"铁律"：不依赖 Flask

打开 [services/__init__.py](services/__init__.py)，它的注释写得很直白：

> services 包：纯业务逻辑层（不依赖 Flask，不处理 HTTP）。只提供可复用的函数，供 blueprints 包中的路由调用。

**为什么必须"不依赖 Flask"？** 三个理由：

1. **可测试**：`list_entries("爬虫基础")` 直接传字符串调用，不需要模拟浏览器请求；
2. **可复用**：`dir_all_images()` 被 browser（目录页）、view（图片预览）两处调用，一处写、处处用；
3. **可替换**：哪天不用 Flask 改用 FastAPI，services 层原封不动搬走，只重写 blueprints 层。

### 5.2 看一个纯函数：dir_utils.natural_key

[dir_utils.py](services/dir_utils.py) 里的 `natural_key` 是"纯逻辑"的完美示范 —— **输入一个名字，输出排序键，没有副作用，不碰网络和磁盘**：

```python
def natural_key(name: str):
    """自然排序键：数字部分按数值比较，其余按文本比较。"""
    return [
        (0, int(part)) if part.isdigit() else (1, part.lower())
        for part in re.split(r"(\d+)", name)
    ]
```

这个函数解决一个经典问题：字符串默认排序 "10" < "2"（字典序），而人类想要 "1, 2, 10, 11"。它用 `re.split(r"(\d+)")` 把名字按数字切块，数字转 int 比较 —— **一个 30 行的函数，解决了一个很实际的体验问题，而且可以单独拿去测试。**

> 注意它的注释风格：先解释"为什么需要它"（问题背景），再讲实现。**注释写"为什么"比写"是什么"更有价值**，这也是你项目里每段注释的风格。

### 5.3 services 之间的内部协作

services 不是孤岛，它们也互相调用，比如：

- `dir_utils` 里调用 `path_utils.safe_path`（列目录前先校验路径）
- `dir_utils` 里调用 `text_utils.read_text_file`（搜索内容时读文件）

**注意依赖方向依然单向**：services 内部的依赖都是"基础工具 → 高级工具"，绝不反向。

---

## 6. 前端层：templates + static

### 6.1 templates —— Jinja2 模板

Flask 用 Jinja2 模板渲染 HTML。本项目模板分工：

- `nav.html` —— **共享导航栏**，被所有页面 `{% include %}` 引用（写一处、处处有）
- `main.html` —— 主页/目录页
- `view.html` —— 查看页
- `search.html` / `editor.html` / `logs.html` / `todos.html` / `gallery.html`

看 main.html 里怎么用模板语法和路由函数：

```html
<a class="fc-dl" href="{{ url_for('download.download_dir', subpath=d.path) }}"
   title="打包下载整本">⬇</a>
```

**`url_for('download.download_dir', ...)` 是蓝图名字 + 函数名的组合** —— 模板不写死 URL，而是"按名字找路由"。这样后端路由地址改了，模板一行都不用动。这是解耦的又一个例子。

### 6.2 static —— CSS/JS 的"组件化"

- `css/common.css` —— 公共设计系统：变量、导航栏、弹窗、暗色模式。所有页面共用
- `js/theme.js` —— 公共主题切换
- `js/player.js` / `gallery.js` / `confirm-download.js` —— 各自页面专用

**原则：公共的抽出来，私有的留页面**。比如下载确认弹窗做成 `confirm-download.js` 后，主页、搜索页、图片页三处下载入口共用同一个组件，不用写三份。

---

## 7. 一次请求的完整旅程

把三层串起来看，一次"浏览目录"请求长这样：

```
用户在浏览器输入 http://127.0.0.1:5000/browse/爬虫基础
        │
        ▼
app.py 里注册的 browser 蓝图收到请求
        │
        ▼
browser.py: browse("爬虫基础")
        │ 调 services
        ▼
dir_utils.py: list_entries("爬虫基础")
        │  内部再调 path_utils.safe_path 校验路径
        │  再调 os 列目录 + natural_key 自然排序
        ▼
返回 [子目录列表, 文件列表]  ← services 层到此为止，不碰 HTTP
        │
        ▼
browser.py: browse_context() 把数据加工成模板上下文 dict
        │
        ▼
render_template("main.html", **ctx)  →  浏览器渲染出页面
```

**注意观察：数据在每一层都"变形"一次 ——**
- services 层：返回原始的"目录里有啥"
- 路由层：加工成"给模板用什么样"（加 url、缩略图、封面信息）
- 模板层：决定"怎么显示"

每一层只改自己该改的部分，这就是分层的意义。

---

## 8. 贯穿全项目的四大设计原则

### 原则 1：单一职责（一个模块只做一件事）

每个文件注释第一句都是"xxx 模块：只做一件事"：
- `path_utils.py` 只做路径校验
- `media.py` 只做返回二进制流
- `download.py` 只做下载

判断标准：**如果一句话说不清这个文件干嘛，说明它塞了太多职责，该拆了。**

### 原则 2：依赖方向单一（上层调下层，不许反）

- 模板 → 蓝图 → 服务，箭头永远向下
- 这也让"找代码"变成纯机械动作：页面问题找模板/蓝图，逻辑问题找 services

### 原则 3：配置与逻辑分离

所有"会变的值"集中在 config.py（路径、扩展名、上限、MIME）。改配置不改逻辑。

### 原则 4：边界做防御，内部做信任

- **边界**（用户输入）：路径要 `safe_path` 校验、上传要限大小、文件名要防 `..`
- **内部**（自己人传参）：信任 services 的返回值，不做多余校验

对应代码：每个路由进来第一件事就是 `safe_path(subpath)` + `abort(404)`。**防御集中在入口，比散落各处好维护。**

---

## 9. 实战演练：给项目加一个新功能

假设你要给项目加"收藏夹"功能：用户可以收藏文件/文件夹，收藏列表显示在导航栏。按本项目的架构，你会这样动手：

### 第 1 步：加配置（config.py）

```python
# 收藏数据存哪
FAVORITE_FILE = os.path.join(BASE_DIR, "收藏夹", "favorites.json")
```

### 第 2 步：写纯逻辑（services/favorite_store.py）

仿照 `log_store.py` 的 JSON 读写模式：

```python
def add_favorite(path: str) -> list:
    """把一个相对路径加入收藏；返回最新列表。"""
    items = load_favorites()          # 读 JSON
    if path not in items:
        items.append(path)
    save_favorites(items)             # 写回 JSON
    return items
```

### 第 3 步：写路由（blueprints/favorites.py）

```python
from flask import Blueprint, request, jsonify
from services import favorite_store

favorites_bp = Blueprint("favorites", __name__)

@favorites_bp.route("/favorite/add", methods=["POST"])
def add():
    path = request.form.get("path", "")
    return jsonify({"items": favorite_store.add_favorite(path)})
```

### 第 4 步：注册蓝图（app.py）

```python
from blueprints.favorites import favorites_bp
app.register_blueprint(favorites_bp)
```

### 第 5 步：加模板/前端（templates/nav.html + static/js）

在导航栏加"⭐ 我的收藏"入口，点开用 JS 拉取收藏列表。

**看出套路了吗？加功能 = 配置 + 逻辑 + 路由 + 注册 + 前端，五步固定动作。** 这就是好架构的回报：新功能有固定的"放法"，不用想半天。

---

## 10. 从本项目学到的通用方法论

把你在这个项目里看到的东西，抽象成下次写任何程序的清单：

1. **先分层再写码**：入口 / 路由 / 逻辑 / 前端，四层先搭好空壳
2. **命名即文档**：文件名、函数名、蓝图名都带职责，别用 `utils`、`misc`、`其他` 这类名字
3. **一个文件只做一件事**，一句话说不清就拆
4. **所有入口先做边界校验**（用户输入不可信）
5. **可变的放配置**，逻辑里不写死路径/阈值/密码
6. **注释写"为什么"**：背景、权衡、坑，而不是翻译代码
7. **公共的抽出来，私有的留页面**（CSS/JS/组件同理）
8. **每删一个功能，要清理它的代码 + 文档 + 配置**（本项目删游戏、删回收站就是这么做的）

### 你的下一步练习建议

1. **抄一遍**：照着 app.py 的骨架自己搭一个最小 Flask 项目（首页 + 一个蓝图 + 一个 service），跑通即可
2. **加个功能**：按第 9 节的方法给本项目加"收藏夹"，亲身体验五步套路
3. **删个功能**：反过来删一个功能，体会"按图索骥"清理代码有多轻松
4. **读官方文档**：Flask 的 Application Factories 和 Blueprints 两章，你会发现自己已经会了

---

## 11. 官方资料推荐

| 主题 | 链接 | 说明 |
| --- | --- | --- |
| Flask 应用工厂 | https://flask.palletsprojects.com/en/stable/patterns/appfactories/ | app.py 用 create_app() 的官方依据 |
| Flask 蓝图 | https://flask.palletsprojects.com/en/stable/blueprints/ | blueprints 分组的官方文档 |
| Flask 模板 | https://flask.palletsprojects.com/en/stable/templating/ | Jinja2 语法与 url_for |
| Python 项目结构（社区最佳实践） | https://docs.python-guide.org/writing/structure/ | 模块怎么组织、包怎么设计 |
| Flask 官方中文教程 | https://dormousehole.readthedocs.io/en/latest/ | 中文翻译版（稍旧，但概念通用） |

---

> 最后记住一句话：**架构不是一次设计好的，是随着功能增长不断"长出来"的。** 你每写一个新功能都按上面的套路放，项目就会自然长成本项目这样清晰的样子。祝你学习顺利！
